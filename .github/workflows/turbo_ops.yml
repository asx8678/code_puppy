name: turbo_ops CI

on:
  push:
    paths:
      - 'turbo_ops/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/turbo_ops.yml'
  pull_request:
    paths:
      - 'turbo_ops/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/turbo_ops.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: Check & Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      
      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: turbo_ops
      
      - name: Check formatting
        run: cargo fmt -p turbo_ops -- --check
      
      - name: Clippy
        run: cargo clippy -p turbo_ops -- -D warnings
      
      - name: Build
        run: cargo build -p turbo_ops --release

  test:
    name: Test
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
      
      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: turbo_ops
      
      - name: Run tests
        run: cargo test -p turbo_ops --release
